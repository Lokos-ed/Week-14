import { createRoot } from 'react-dom/client'
import App from './App.tsx'

//Initializes everything by creating a root element, and creating an instance of <App> within it
createRoot(document.getElementById('root')!).render(
  
    <App />
  
)
